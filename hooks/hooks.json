{
  "description": "Session-start version check: reports newer releases, safely applies an opted-in repository-scoped update, and says when restart is required. Silent when current; never blocks a session.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume",
        "hooks": [
          { "type": "command", "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/version-check.sh\"" }
        ]
      }
    ]
  }
}
