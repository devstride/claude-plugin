{
  "description": "Session-start version check: reports newer releases, safely applies an opted-in repository-scoped update, and gives a verified reload handoff. Silent when current; every operation is bounded.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume",
        "hooks": [
          { "type": "command", "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/version-check.sh\"" }
        ]
      }
    ]
  }
}
